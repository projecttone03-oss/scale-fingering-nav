# スケール運指ナビ

がくふ工房「管楽器奏者のための基礎トレーニング」対応の Web アプリ。
全24調スケールをメトロノーム・参考音と同期させ、「今の音／次の音」の楽譜と運指を表示する。

## 最初に読むもの
- docs/SPEC.md ― 唯一の仕様書。実装前に必ず全文を読む。仕様の解釈で迷ったら SPEC.md と、PDF「基礎トレーニング」の記載を正とする。
- docs/research/ ― 運指の調査資料。**参考情報であり「正」ではない**。金管・トロンボーンは PDF の運指番号／ポジションが正、木管はメーカー公式運指表と突き合わせてから採用する。

## コマンド
- `npm run dev` 開発サーバー
- `npm test` 単体テスト（Vitest）
- `npm run validate` データ検証（SPEC 10.2）。データを変更したら必ず通す
- `npm run build` 本番ビルド

## 守ること
- UI フレームワーク・記譜ライブラリ・オーディオライブラリを追加しない（Vite + TypeScript + DOM/SVG + Web Audio API のみ）
- 運指図は楽器ごとの独自テンプレート SVG（src/templates/）だけを使う。外部サイトの図版・画像・PDF をリポジトリに入れない、トレースしない
- 楽譜データ（src/data/scales/）は PDF から転記する。計算で生成したものをそのまま採用しない
- UI の状態は store の `progress` 1つから導出する（SPEC 7.1）。setTimeout で UI を更新しない
- 縦向きスマホ（幅 375px・高さ 812px）で S2 がスクロールなしに収まることを常に確認する
- コミット前に `npm test` と `npm run validate` を通す
- 日本語で応答する

## 進め方
docs/SPEC.md 9章のマイルストーン順に進める。指示されたマイルストーンの範囲を超えて先回りしない。
